#include <iostream>

using namespace std;

int main()
{

    int a = 10;
    int b = 20;

    // CONDITION ? so instruction will be executed if condition is true : if consition is not true

    string message = ( a > b) ? " a > b" : " a <= b";

    cout << message << endl;


    return 0;
}